package com.example.wallnoire.presentation.common.holder

import com.example.wallnoire.databinding.ItemLoadMoreBinding
import com.example.wallnoire.base.adapter.BaseViewHolder

class LoadMoreViewHolder(private val binding: ItemLoadMoreBinding) : BaseViewHolder<Unit>(binding.root) {
    override fun bind(data: Unit) {}
}